# Asset inventory

Deployable media is stored under `public/assets/projects/` so the same files work in both Sites and GitHub Pages builds.

## Robot joint bracket

The six project images are rendered from the verified 9-page robot bracket portfolio and cover design inputs, the V1–V3 comparison, three load cases, the governing case, mesh sensitivity and the Python verification workflow.

## Ball valve

The seven project images are rendered from the verified 9-page ball-valve engineering portfolio and cover the design boundary, six-component architecture, model reduction, mesh, load cases, result comparison and governing case.

No ANSYS project database, complete CAD model or other editable proprietary engineering file is included.

